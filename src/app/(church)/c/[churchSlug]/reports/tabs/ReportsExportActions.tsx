import { InlineAlert } from "@/components/ui/InlineAlert";

export function ReportsExportActions() {
  return (
    <InlineAlert
      variant="info"
      message="PDF export, Excel export, and print summary are coming soon."
    />
  );
}
